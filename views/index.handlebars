<div>Hello, World!</div>
{{#each allCards}}
  {{#with this.card}}
    <div>{{name}}</div>
    <div>{{text}}</div>
    <div>{{powerCost}}</div>
  {{/with}}
{{/each}}

<form method='post' action='login/logout'>
  <button type='submit'>Logout</button>
</form>
